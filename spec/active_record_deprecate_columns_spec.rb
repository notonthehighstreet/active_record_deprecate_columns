require 'spec_helper'

describe ActiveRecordDeprecateColumns do
  it 'has a version number' do
    expect(ActiveRecordDeprecateColumns::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
